{
  "name": "sass-inline-svg-test",
  "description": "Inline some SVGs in Sass using node-sass and sass-inline-svg",
  "version": "1.0.0",
  "author": "Peter deHaan <peter@deseloper.com> (https://about.me/peterdehaan)",
  "dependencies": {
    "node-sass": "3.8.0",
    "sass-inline-svg": "0.1.0"
  },
  "devDependencies": {
    "eslint": "3.1.1",
    "rimraf": "2.5.4"
  },
  "keywords": [],
  "license": "WTFPL",
  "main": "index.js",
  "private": true,
  "scripts": {
    "api": "node index",
    "cli": "node-sass static/main.scss --functions ./funcs.js -o dist2",
    "clean": "rimraf dist/*.css dist2/*.css",
    "lint": "eslint . ",
    "pretest": "npm run lint && npm run clean",
    "test": "npm run api && npm run cli && diff dist/main.css dist2/main.css"
  }
}
